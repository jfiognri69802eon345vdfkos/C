#include <stdio.h>
#include <stdlib.h>

#ifndef SORT_H_INCLUDED
#define SORT_H_INCLUDED

void Bubblesort(int*, int);
void Mergesort(int, int*);
void mergeList(int*,int*,int*,int,int,int);
void flip(int*, int*);
void umdrehen(int*, int);

#endif
